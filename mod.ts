/**
 * A module providing a function to generate a random color by hexadecimal string.
 *
 * @example
 * ```ts
 * import { colorandom } from "@k8o/colorandom";
 *
 * colorandom();
 * ```
 *
 * @module
 */

export * from "./main.ts";
